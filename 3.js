 //                       adding of two strings

 let a = "hello";
 let b = "world";

 console.log(a+b);// ans is "helloworld". The + sign concat two strings.

 let x = 5;
 let y = `5`;

 console.log(x+y); // the answer will be 55 as it only joins two strings together.
 
//  x++;
//  console.log(x);// the answer will be 6 as `x++` adds up a number.

//  x--;
//  console.log(x); // the answer will be 4 as `x--` substracts 1 from the actual value of x ;

 x+=3;
 console.log(x);// output will be 8 as it represents to be as x=x+3; 

 let _a = "hi";
 let _b = "afnaan";
 
 console.log(_a +" " + _b);// we can merge two strings by + sign.
 

